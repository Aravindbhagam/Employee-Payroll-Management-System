name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  server:
    name: Server (syntax check + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: payrollpro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/payrollpro_test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      # The server is plain JavaScript (native ES modules, no build step) --
      # `node --check` catches syntax errors before the slower test run.
      - name: Syntax-check all JS files
        run: |
          status=0
          for f in $(find src prisma tests vitest.config.js -name '*.js'); do
            node --check "$f" || status=1
          done
          exit $status

      - name: Run tests
        # tests/globalSetup.js resets the schema of the Postgres service
        # container above via `prisma migrate reset`; JWT secrets are
        # hardcoded in tests/setup.js, so no repository secrets are
        # required here.
        run: npm test

  client:
    name: Client (syntax check)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # The client is plain HTML/CSS/JS with no build step (Tailwind loads
      # via CDN, ES modules load directly in the browser) -- `node --check`
      # is a cheap way to still catch syntax errors before they ship.
      - name: Syntax-check all JS modules
        run: |
          status=0
          for f in $(find src -name '*.js'); do
            node --input-type=module --check < "$f" || status=1
          done
          exit $status
